name: pipeline-env
channels:
  - conda-forge
  - defaults
dependencies:
  - pip:
    - matplotlib
    - seaborn
    - cython
    - numpy
    - numba
    - scipy
    - scikit-learn
    - statsmodels
    - pandas
